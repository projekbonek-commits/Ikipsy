import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paypal.ball',
  appName: 'PayPal Ball',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;

